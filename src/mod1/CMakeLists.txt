# CMakeLists.txt in src/main
# Create a library for the main module
# add_library(mod1_module mod1.cpp mod1.h)

# Include directories for this module, if needed
# target_include_directories(mod1_module PRIVATE ${PROJECT_SOURCE_DIR})